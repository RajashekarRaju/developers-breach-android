<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="navController"
            type="androidx.navigation.NavController" />

        <variable
            name="viewModel"
            type="com.developerbreach.developerbreach.view.commonWebView.CommonWebViewViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/background"
        android:fitsSystemWindows="true"
        android:theme="@style/AppTheme.MainTheme"
        tools:context=".view.commonWebView.CommonWebViewFragment">

        <ImageView
            android:id="@+id/fragment_common_web_view_up_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="20dp"
            android:alpha="0.7"
            android:background="?selectableItemBackgroundBorderless"
            android:contentDescription="@string/up_button_content_description"
            android:onClick="@{() -> navController.navigateUp()}"
            android:src="@drawable/ic_close"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="?attr/colorOnBackground" />

        <TextView
            android:id="@+id/fragment_common_web_view_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="4dp"
            android:maxLines="1"
            android:text="@{viewModel.toolBarTitle}"
            android:textAppearance="?attr/textAppearanceHeadline5"
            android:textColor="?attr/colorOnBackground"
            app:layout_constraintBottom_toBottomOf="@id/fragment_common_web_view_up_button"
            app:layout_constraintStart_toEndOf="@id/fragment_common_web_view_up_button"
            app:layout_constraintTop_toTopOf="@id/fragment_common_web_view_up_button"
            tools:text="Title" />

        <ProgressBar
            android:id="@+id/fragment_common_web_view_progress_bar"
            style="@style/ProgressBar.Article.Material.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/fragment_common_web_view_up_button"
            tools:visibility="visible" />

        <WebView
            android:id="@+id/fragment_common_web_view_content"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="16dp"
            app:bindCommonWebViewClient="@{fragmentCommonWebViewProgressBar}"
            app:bindCommonWebViewUrl="@{viewModel}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/fragment_common_web_view_up_button" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>